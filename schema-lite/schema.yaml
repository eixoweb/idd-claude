name: idd-claude-lite
version: 1
description: Bounded-change OpenSpec workflow for Claude Code - proposal ->
  specs -> tasks -> verification
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: >
      This is the bounded workflow: no design document, no ADR. If while writing
      this proposal it becomes clear that the change is cross-cutting,
      introduces a new external dependency, carries security or migration
      complexity, or needs an architectural decision recorded, stop and tell the
      user to recreate the change with the full schema: openspec new change <id>
      --schema idd-claude



      Create the proposal document that establishes WHY this change is needed.


      Sections:

      - **Why**: 1-2 sentences on the problem or opportunity. What problem does
      this solve? Why now?

      - **What Changes**: Bullet list of changes. Be specific about new
      capabilities, modifications, or removals. Mark breaking changes with
      **BREAKING**.

      - **Capabilities**: Identify which behaviour specs will be created or
      modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names (e.g., `user-auth`, `data-export`).
        - **Modified Capabilities**: List existing capabilities whose behaviour is changing. Only include if spec-level behaviour changes, not just implementation details. Each needs an OpenSpec Markdown delta file at `specs/<name>/spec.md`. Check `openspec/specs/` for existing spec names. Leave empty if no behaviour changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.


      IMPORTANT: The Capabilities section is critical. It creates the contract
      between proposal and specs phases. Research existing specs before filling
      this in. Each capability listed here will need a corresponding spec file.


      Durable architectural decisions worth carrying across changes are
      distilled into ADR files at <repo>/adr/ by a later step after design.


      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      implementation details belong in design.md.


      This is the foundation - specs, design, adr, and tasks all build on this.
    requires: []
  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change
    template: spec.md
    instruction: >
      Create specification files that define WHAT the system should do.


      Create one spec file per capability listed in the proposal's Capabilities
      section.

      - New capabilities: use the exact kebab-case name from the proposal
      (specs/<capability>/spec.md).

      - Modified capabilities: use the existing spec folder name from
      openspec/specs/<capability>/ when creating the delta spec at
      specs/<capability>/spec.md.


      Delta operations (use ## headers):

      - **ADDED Requirements**: New capabilities

      - **MODIFIED Requirements**: Changed behavior - MUST include full updated
      content

      - **REMOVED Requirements**: Deprecated features - MUST include **Reason**
      and **Migration**

      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format


      Format requirements:

      - Each requirement: `### Requirement: <name>` followed by description

      - Use SHALL/MUST for normative requirements (avoid should/may)

      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format

      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3
      hashtags or bullets will fail silently.

      - Every requirement MUST have at least one scenario.


      MODIFIED requirements workflow:

      1. Locate the existing requirement in openspec/specs/<capability>/spec.md

      2. Copy the ENTIRE requirement block (from `### Requirement:` through all
      scenarios)

      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior

      4. Ensure header text matches exactly (whitespace-insensitive)


      Common pitfall: Using MODIFIED with partial content loses detail at
      archive time.

      If adding new concerns without changing existing behavior, use ADDED
      instead.


      Example:

      ```

      ## ADDED Requirements


      ### Requirement: User can export data

      The system SHALL allow users to export their data in CSV format.


      #### Scenario: Successful export

      - **WHEN** user clicks "Export" button

      - **THEN** system downloads a CSV file with all user data


      ## REMOVED Requirements


      ### Requirement: Legacy export

      **Reason**: Replaced by new export system

      **Migration**: Use new export endpoint at /api/v2/export

      ```


      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      **Every task carries a keyword after its ordinal**, before the description: `- [ ] 1.2 GREEN — implement requestToken`. `/idd:apply` dispatches on that keyword and on nothing else, so a task without one is a task it does not know how to run.

      | Keyword | Use it for |
      | --- | --- |
      | `RED` | the failing test, written first, with the failure mode named in the description |
      | `GREEN` | the minimal code that makes the preceding RED pass |
      | `REFACTOR` | cleanup at constant behaviour — its diff must touch no test assertion |
      | `VISUAL` | measured dev-browser assertions against a rendered page |
      | `ACCEPT` | one Gherkin scenario — only when `spec_as_source` is on |

      **Every GREEN is preceded by its RED, in the same group.** Writing the code first and its test after is the inversion this workflow exists to prevent: a test written against code that already passes agrees with whatever that code happens to do. One behaviour at a time, RED then GREEN.

      A `VISUAL` task declares its assertions on indented lines beneath it — a `url:`, an optional `viewport:`, then `assert` and `count` lines. Separators are two or more spaces. Prose is not an assertion: write `count .grid > *  12`, never "12 columns". A `VISUAL` task with no assertion is rejected.

      Guidelines:
      - Group related tasks under ## numbered headings.
      - Each task MUST be a checkbox: `- [ ] X.Y KEYWORD - Task description`.
      - Tasks should be small enough to complete in one session.
      - Order tasks by dependency (what must be done first?).
      - If implementation changes behaviour specs, include a task to run `openspec validate <change> --type change --strict` before archive.

      Reference specs for what to build, design for how to build it, and <repo>/adr/ for durable architectural commitments to honor.
      Each task should be verifiable - you know when it's done.
    requires:
      - specs
  - id: verification
    generates: verification.md
    description: Evaluator verdict and iteration log for the change
    template: verification.md
    requires:
      - tasks
    instruction: >
      Record the evaluator verdict for each task group.


      One entry per group and per attempt: the dimension scores, the floors in
      force, the verdict, and the findings that produced it. A group is only
      recorded once its verdict is PASS or once the iteration cap is reached.


      Never write this file by hand during implementation - it is produced by
      the evaluator subagent.
apply:
  requires:
    - tasks
  tracks: tasks.md
  instruction: >
    Do not implement these tasks directly. Run /idd:apply, which enforces the
    TDD gates and dispatches the evaluator at the end of each task group.


    Implementing without it produces a change that has never been verified
    against its own specs.
